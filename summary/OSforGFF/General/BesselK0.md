# `BesselK0.lean` — Informal Summary

> **Source**: [`OSforGFF/General/BesselK0.lean`](../../OSforGFF/General/BesselK0.lean)
> **Generated**: 2026-07-03 (dimension-generic library, `d = 2` instance support)
> **Note**: Auto-generated by `/lean-summarize`. Re-run to refresh.

## Overview

This file defines the modified Bessel function $K_0(z)$ via its cosh integral representation
$K_0(z) = \int_0^\infty e^{-z\cosh t}\,dt$ and proves the single analytic identity the
two-dimensional propagator instance needs: the Schwinger proper-time evaluation
$$\int_0^\infty \frac{1}{t}\,e^{-m^2 t - r^2/(4t)}\,dt = 2\,K_0(mr)
\qquad (m, r > 0)$$
(Gradshteyn–Ryzhik 3.471.9). The evaluation proceeds by the linear scaling $t = (r/2m)s$,
which turns the integrand into the symmetric kernel $(1/s)\,e^{-a(s+1/s)}$ with $a = mr/2$;
the reciprocal involution $s \mapsto 1/s$ folds $(0,\infty)$ onto $(1,\infty)$ (doubling the
integral); and the substitution $s = e^t$ matches the cosh representation of $K_0$.

**Role in the dimension-generic library.** This is the $K_0$ analogue of the $K_1$ theory in
[`General/BesselFunction.lean`](BesselFunction.md): dimension-specific support for the $d = 2$
instance. The two-dimensional radial covariance profile is
$\mathrm{Cprofile}(r) = \tfrac{1}{2\pi}K_0(mr)$, and
[`OSforGFF/Instances/Dim2.lean`](../../OSforGFF/Instances/Dim2.lean) consumes exactly
`schwingerIntegral_eq_besselK0` to discharge the generic `GFFPropagator.schwinger_eq` bridge
obligation. No positivity/continuity/decay theory for $K_0$ is needed — those facts are
supplied generically by `properTimeCovariance` through the typeclass.

## Status

**Main result**: Fully proven — file is sorry-free.

**Length**: 233 lines, 1 definition(s) + 1 public theorem(s) (+ 8 private lemmas)

---

## The modified Bessel function $K_0$

### [`besselK0`](../../OSforGFF/General/BesselK0.lean#L35) — Definition

**Lean signature**
```lean
noncomputable def besselK0 (z : ℝ) : ℝ :=
  ∫ t : ℝ in Ici 0, exp (-z * cosh t)
```
**Informal**: The modified Bessel function of the second kind of order $0$ via its cosh
integral representation $K_0(z) = \int_0^\infty e^{-z\cosh t}\,dt$; well-defined and positive
for $z > 0$.

---

## The Schwinger proper-time evaluation

### [`schwingerIntegral_eq_besselK0`](../../OSforGFF/General/BesselK0.lean#L179) — Theorem

**Statement**: For $m, r > 0$,
$$\int_0^\infty \frac{1}{t}\,e^{-m^2 t - r^2/(4t)}\,dt = 2\,K_0(mr).$$

**Proof route** (three steps, each with its own integrability input):
1. *Linear scaling* $t = (r/2m)s$ (`integral_comp_mul_left_Ioi`): the scale-invariant measure
   $dt/t$ turns the integrand into the symmetric kernel $h(s) = (1/s)e^{-a(s+1/s)}$, $a = mr/2$.
2. *Reciprocal involution* (`self_reciprocal_integral`, private): the substitution $s \mapsto 1/s$
   (`integral_comp_inv_Ioi`) maps $(0,1)$ onto $(1,\infty)$ and fixes $h$, so
   $\int_0^\infty h = 2\int_1^\infty h$.
3. *Exponential substitution* $s = e^t$ (`integral_comp_exp_Ioi_helper`, private):
   $h(e^t)e^t = e^{-2a\cosh t}$, identifying $\int_1^\infty h$ with $K_0(2a) = K_0(mr)$.

Integrability of the kernel is proved from the Gaussian domination
$e^{-z\cosh t} \le e^{-z}e^{-zt^2/2}$ (private lemmas `cosh_ge_one_add_sq_div_two`,
`exp_neg_cosh_le_gaussian`, `integrableOn_gaussian_Ici`) near infinity and from the elementary
bound $te^{-t} \le 1$ (`mul_exp_neg_le_one`, `schwinger_integrand_integrableOn_Ioc`) near the
endpoint.

**Consumed by**:
[`properTimeCovariance_dim2_eq`](../../OSforGFF/Instances/Dim2.lean#L34) — the `d = 2`
proper-time closed form.

---

*This file has **1** definition and **1** public theorem (+ 8 private lemmas; 0 with sorry).*
