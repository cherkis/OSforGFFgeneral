# `Dim3.lean` — Informal Summary

> **Source**: [`OSforGFF/Instances/Dim3.lean`](../../OSforGFF/Instances/Dim3.lean)
> **Generated**: 2026-07-03 (updated for the dimension-generic library)
> **Note**: Auto-generated by `/lean-summarize`. Re-run to refresh.

## Overview

The three-dimensional instance of the `GFFPropagator` typeclass. The radial profile
`Cprofile` of the free covariance at $d = 3$ is the Yukawa potential
$$C(r) = \frac{e^{-mr}}{4\pi r},$$
and its identification with the generic proper-time (Schwinger/heat-kernel) integral
`properTimeCovariance` — the `schwinger_eq` obligation of the typeclass — is proved here by
evaluating the proper-time integral in closed form. Rather than the four-dimensional route
through the modified Bessel function $K_1$, the $d = 3$ evaluation uses the **reciprocal
substitution** $t = 1/s$ (mathlib's `integral_comp_rpow_Ioi` at $p = -1$) to reduce
$$\int_0^\infty t^{-3/2}\,e^{-m^2 t - r^2/(4t)}\,dt$$
to the Bessel-$K_{1/2}$ Laplace identity `LaplaceIntegral.laplace_integral_half_power` (with
parameters $a = m^2$, $b = r^2/4$), giving the closed value $(2\sqrt{\pi}/r)\,e^{-mr}$. The file
supplies the `Fact (2 ≤ 3)` and `Fact (3 ≤ 5)` instances, the `GFFPropagator 3 m` instance, and the
shorthand `μ_GFF3` for the three-dimensional free GFF measure. (The ultraviolet divergence
`C(x_0, x) → +∞` as $x \to x_0$ is now the dimension-generic `freeCovariance_tendsto_atTop` in
`OS/NonTrivial.lean`, which specialises to $d = 3$.)

## Status

**Main result**: Fully proven

**Major gaps**: None — file is sorry-free.

**Length**: 115 lines, 4 definition(s) + 3 theorem(s)/lemma(s)

---

## Proper-time evaluation (Schwinger → Yukawa)

### [`schwingerIntegral_dim3`](../../OSforGFF/Instances/Dim3.lean#L27) — Theorem

**Statement**: The three-dimensional proper-time integral in closed form: for $m, r > 0$,
$$\int_0^\infty t^{-3/2}\,e^{-m^2 t - r^2/(4t)}\,dt = \frac{2\sqrt{\pi}}{r}\,e^{-mr}.$$
Proved by the reciprocal substitution $t = 1/s$ (`integral_comp_rpow_Ioi` at $p = -1$), which turns
the integrand into the $K_{1/2}$ Laplace integrand with $a = m^2$, $b = r^2/4$.

**Proof uses**: `integral_comp_rpow_Ioi`,
[`LaplaceIntegral.laplace_integral_half_power`](../../OSforGFF/General/LaplaceIntegral.lean#L542)

---

### [`heatKernelProfile_dim3`](../../OSforGFF/Instances/Dim3.lean#L60) — Lemma

**Statement**: The three-dimensional heat-kernel profile with the constant $(4\pi)^{-3/2}$ pulled
out: for $t > 0$,
$$H_3(t, r) = (4\pi)^{-3/2}\,t^{-3/2}\,e^{-r^2/(4t)}.$$

**Proof uses**: `Real.mul_rpow` *(the $(4\pi t)^{-3/2} = (4\pi)^{-3/2} t^{-3/2}$ split)*

---

### [`properTimeCovariance_dim3_eq`](../../OSforGFF/Instances/Dim3.lean#L68) — Theorem

**Statement**: The three-dimensional proper-time covariance is the Yukawa profile: for $m, r > 0$,
$$\mathrm{properTimeCovariance}\;3\;m\;r = \frac{e^{-mr}}{4\pi r}.$$
Obtained by extracting the constant $(4\pi)^{-3/2}$ and applying `schwingerIntegral_dim3`, using the
identity $(4\pi)^{-3/2}\cdot 2\sqrt{\pi} = 1/(4\pi)$.

**Proof uses**: [`heatKernelProfile_dim3`](../../OSforGFF/Instances/Dim3.lean#L60),
[`schwingerIntegral_dim3`](../../OSforGFF/Instances/Dim3.lean#L27), `integral_const_mul`

---

## The three-dimensional `GFFPropagator` instance

### [`instFact_2_le_3`](../../OSforGFF/Instances/Dim3.lean#L99) — Instance

**Lean signature**
```lean
instance : Fact ((2 : ℕ) ≤ 3)
```
**Informal**: The `Fact (2 ≤ 3)` instance needed for the time/space split (`2 ≤ d`).

---

### [`instFact_3_le_5`](../../OSforGFF/Instances/Dim3.lean#L102) — Instance

**Lean signature**
```lean
instance : Fact ((3 : ℕ) ≤ 5)
```
**Informal**: The `Fact (3 ≤ 5)` instance; the bound $d \le 5$ enters the OS3 proper-time Fubini
domination.

---

### [`instGFFPropagatorDim3`](../../OSforGFF/Instances/Dim3.lean#L106) — Instance

**Lean signature**
```lean
noncomputable instance instGFFPropagatorDim3 (m : ℝ) [Fact (0 < m)] :
    GFFPropagator 3 m
```
**Informal**: The `GFFPropagator 3 m` instance for $m > 0$. Its radial profile is the Yukawa closed
form
$$\mathrm{Cprofile}(r) = \frac{e^{-mr}}{4\pi r} \quad (r \ne 0),$$
regularised to $0$ at $r = 0$. The `schwinger_eq` field — that for $r > 0$ this profile equals the
generic proper-time integral at $d = 3$ — is discharged by `properTimeCovariance_dim3_eq`.

**Proof uses**: [`properTimeCovariance_dim3_eq`](../../OSforGFF/Instances/Dim3.lean#L68)

---

### `μ_GFF3` — Definition (abbrev)

**Lean signature**
```lean
@[simp] abbrev μ_GFF3 (m : ℝ) [Fact (0 < m)] :=
  gaussianFreeField_free (d := 3) m
```
**Informal**: Shorthand for the free Gaussian Free Field probability measure of the three-dimensional
instance, i.e. [`gaussianFreeField_free`](../../OSforGFF/Measure/Construct.lean#L143) specialised to
`d = 3`.

---

*This file has **4** definitions and **3** theorems/lemmas (0 with sorry).*
