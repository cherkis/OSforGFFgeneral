# `Dim4.lean` — Informal Summary

> **Source**: [`OSforGFF/Instances/Dim4.lean`](../../OSforGFF/Instances/Dim4.lean)
> **Generated**: 2026-07-03 (updated for the dimension-generic library)
> **Note**: Auto-generated by `/lean-summarize`. Re-run to refresh.

## Overview

The four-dimensional instance of the `GFFPropagator` typeclass. The radial profile
`Cprofile` of the free covariance at $d = 4$ is the Bessel closed form
$$C(r) = \frac{m}{4\pi^2 r}\,K_1(mr),$$
and its identification with the generic proper-time (Schwinger/heat-kernel) integral
`properTimeCovariance` — the `schwinger_eq` obligation of the typeclass — is exactly the
Schwinger-representation evaluation `covarianceSchwingerRep_eq_besselFormula` from
`Instances/Dim4Bessel.lean`. At this instance the generic kernel
`freeCovariance STDimension m` coincides definitionally with the four-dimensional Bessel
kernel `freeCovariance4 m`. The file also introduces the shorthand `μ_GFF` for the free
GFF probability measure of the $d = 4$ instance.

## Status

**Main result**: Fully proven

**Major gaps**: None — file is sorry-free.

**Length**: 41 lines, 2 definition(s) + 1 theorem(s)/lemma(s)

---

## The four-dimensional `GFFPropagator` instance

### [`instGFFPropagatorDim4`](../../OSforGFF/Instances/Dim4.lean#L27) — Instance

**Lean signature**
```lean
noncomputable instance instGFFPropagatorDim4 (m : ℝ) [Fact (0 < m)] :
    GFFPropagator STDimension m
```
**Informal**: The `GFFPropagator STDimension m` instance for $m > 0$. Its radial profile is the Bessel closed form
$$\mathrm{Cprofile}(r) = \frac{m}{4\pi^2 r}\,K_1(mr) \quad (r \ne 0),$$
regularised to $0$ at $r = 0$. The `schwinger_eq` field — that for $r > 0$ this profile equals the generic proper-time integral $\int_0^\infty e^{-tm^2}(4\pi t)^{-d/2}e^{-r^2/(4t)}\,dt$ at $d = 4$ — is discharged by rewriting with `covarianceSchwingerRep_eq_besselFormula` and `rfl`.

**Proof uses**: [`covarianceSchwingerRep_eq_besselFormula`](../../OSforGFF/Instances/Dim4Bessel.lean#L396)

---

### [`freeCovariance_dim4_eq`](../../OSforGFF/Instances/Dim4.lean#L35) — Lemma

**Statement**: At $d = 4$ the generic position-space kernel is definitionally the Bessel kernel: for all $x, y \in \mathbb{R}^4$,
$$\mathrm{freeCovariance}\;\mathrm{STDimension}\;m\;x\;y = \mathrm{freeCovariance4}\;m\;x\;y.$$

**Proof uses**: *(definitional — `rfl`)*

---

### `μ_GFF` — Definition (abbrev)

**Lean signature**
```lean
@[simp] abbrev μ_GFF (m : ℝ) [Fact (0 < m)] :=
  gaussianFreeField_free (d := STDimension) m
```
**Informal**: Shorthand for the free Gaussian Free Field probability measure of the four-dimensional instance, i.e. [`gaussianFreeField_free`](../../OSforGFF/Measure/Construct.lean#L143) specialised to `d = STDimension`.

---

*This file has **2** definitions and **1** theorem/lemma (0 with sorry).*
