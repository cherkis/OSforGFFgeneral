# `Dim5.lean` — Informal Summary

> **Source**: [`OSforGFF/Instances/Dim5.lean`](../../OSforGFF/Instances/Dim5.lean)
> **Generated**: 2026-07-04 (dimension-generic library)
> **Note**: Auto-generated by `/lean-summarize`. Re-run to refresh.
> **⚠ Stale (pre–Stage G)**: predates the uniform-`d` refactor and still lists removed identifiers (`STDimension`, the `*4` shorthands, and/or `μ_GFF2`/`μ_GFF3`/`μ_GFF5`). The Lean source is authoritative; regenerate with `/lean-summarize`.

## Overview

The five-dimensional instance of the `GFFPropagator` typeclass. The radial profile `Cprofile` of
the free covariance at $d = 5$ is the $K_{3/2}$ profile
$$C(r) = \frac{(1 + mr)\,e^{-mr}}{8\pi^2 r^3},$$
and its identification with the generic proper-time (Schwinger/heat-kernel) integral
`properTimeCovariance` — the `schwinger_eq` obligation — is proved as the order $\nu = -3/2$ case
of the master Bessel identity `schwingerIntegral_eq_besselK` (`General/BesselK`). The evaluation
uses the half-integer value
$$K_{3/2}(z) = \sqrt{\tfrac{\pi}{2z}}\,e^{-z}\Bigl(1 + \tfrac{1}{z}\Bigr),$$
proved here as `besselK_three_half` via the identity $\cosh(3t/2) = \cosh(t/2)(1 + 4\sinh^2(t/2))$,
the substitution $u = \sinh(t/2)$, and the Gaussian zeroth and second moments
(`integral_gaussian_Ioi`, `gaussian_moment2`).

`d = 5` is the boundary of the OS3 proper-time Fubini envelope ($s^{(4-d)/2} = s^{-1/2}$, still
integrable), so it sits inside the proven `d ≤ 5` range and the generic master theorem applies. The
file supplies the `Fact (5 ≤ 5)` instance (reusing `Instances.Dim2`'s `Fact (2 ≤ 5)`), the
`GFFPropagator 5 m` instance, and the shorthand `μ_GFF5`.

## Status

**Main result**: Fully proven

**Major gaps**: None — file is sorry-free.

---

## Key declarations

- [`gaussian_moment2`](../../OSforGFF/Instances/Dim5.lean) — the Gaussian second moment
  $\int_0^\infty u^2 e^{-b u^2}\,du = \sqrt{\pi}/(4 b^{3/2})$, via mathlib's
  `integral_rpow_mul_exp_neg_mul_rpow` and $\Gamma(3/2) = \sqrt{\pi}/2$.
- [`besselK_three_half`](../../OSforGFF/Instances/Dim5.lean) — the closed-form $K_{3/2}(z)$.
- [`properTimeCovariance_dim5_eq`](../../OSforGFF/Instances/Dim5.lean) — the closed-form
  proper-time covariance $(1+mr)e^{-mr}/(8\pi^2 r^3)$.
- `instGFFPropagatorDim5`, `μ_GFF5` — the instance and measure shorthand.

The five-dimensional headline `gaussianFreeField_satisfies_all_OS_axioms_dim5` (in `OS/Master.lean`)
is the corresponding instance of `gaussianFreeField_satisfies_all_OS_axioms_generic`; its
3-core-axiom footprint and statement type are build-frozen in `Guardrails.lean`.
