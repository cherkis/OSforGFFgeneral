# `Basic.lean` — Informal Summary

> **Source**: [`OSforGFF/Spacetime/Basic.lean`](../../OSforGFF/Spacetime/Basic.lean)
> **Generated**: 2026-07-03 (updated for the dimension-generic library)
> **Note**: Auto-generated by `/lean-summarize`. Re-run to refresh.
> **⚠ Stale (pre–Stage G)**: predates the uniform-`d` refactor and still lists removed identifiers (`STDimension`, the `*4` shorthands, and/or `μ_GFF2`/`μ_GFF3`/`μ_GFF5`). The Lean source is authoritative; regenerate with `/lean-summarize`.

## Overview

Establishes the core type definitions used throughout the formalization of the
Glimm--Jaffe OS axioms, parameterized by the spacetime dimension `d : ℕ`.
Introduces `SpaceTime d` as Euclidean $\mathbb{R}^d$, Schwartz-class test
functions (real and complex), tempered distributions as field configurations,
and the generating functional $Z[J] = \int e^{i\langle\omega,J\rangle}\,d\mu(\omega)$.
Also defines spatial geometry helpers (spatial coordinates $\mathbb{R}^{d-1}$,
$L^2$ space on slices) and the on-shell energy $E(m,k) = \sqrt{\lVert k\rVert^2 + m^2}$.
The time/space split requires the hypothesis `[Fact (2 ≤ d)]`. Four-dimensional
shorthands (`SpaceTime4`, `TestFunction4`, …) name the `d = STDimension = 4` instance.

## Status

**Main result**: Fully proven — file is sorry-free.

**Length**: 302 lines, 27 definition(s) + 10 theorem(s)/lemma(s)

---

## Basic Definitions

### [`SpaceTime`](../../OSforGFF/Spacetime/Basic.lean#L57) — Definition

**Lean signature**
```lean
abbrev SpaceTime (d : ℕ) := EuclideanSpace ℝ (Fin d)
```
**Informal**: Euclidean spacetime $\mathbb{R}^d$ equipped with its standard $L^2$ inner-product norm.

---

### [`STDimension`](../../OSforGFF/Spacetime/Basic.lean#L60) — Definition

**Lean signature**
```lean
abbrev STDimension := 4
```
**Informal**: The spacetime dimension of the concrete four-dimensional instance, fixed to $4$.

---

### [`SpaceTime4`](../../OSforGFF/Spacetime/Basic.lean#L63) — Definition

**Lean signature**
```lean
abbrev SpaceTime4 := SpaceTime STDimension
```
**Informal**: Four-dimensional Euclidean spacetime $\mathbb{R}^4$, the `d = 4` instance of `SpaceTime`.

---

### [`Fact (2 ≤ STDimension)`](../../OSforGFF/Spacetime/Basic.lean#L65) — Instance

**Lean signature**
```lean
instance : Fact (2 ≤ STDimension)
```
**Informal**: Records $2 \le 4$ as a `Fact`, so the four-dimensional instance satisfies the standing hypothesis of the time/space split.

---

### [`Fact (STDimension ≤ 5)`](../../OSforGFF/Spacetime/Basic.lean#L67) — Instance

**Lean signature**
```lean
instance : Fact (STDimension ≤ 5)
```
**Informal**: Records $4 \le 5$ as a `Fact` (upper dimension bound used by the propagator estimates).

---

### [`NeZero d`](../../OSforGFF/Spacetime/Basic.lean#L70) — Instance

**Lean signature**
```lean
instance {d : ℕ} [Fact (2 ≤ d)] : NeZero d
```
**Informal**: Dimensions admitting a time/space split are nonzero, so the index $(0 : \mathrm{Fin}\ d)$ is available.

---

### [`getTimeComponent`](../../OSforGFF/Spacetime/Basic.lean#L75) — Definition

**Lean signature**
```lean
abbrev getTimeComponent {d : ℕ} [Fact (2 ≤ d)] (x : SpaceTime d) : ℝ
```
**Informal**: Projects $x \in \mathbb{R}^d$ onto its zeroth (time) coordinate $x_0$; the split needs $d \ge 2$.

---

### [`TestFunction`](../../OSforGFF/Spacetime/Basic.lean#L91) — Definition

**Lean signature**
```lean
abbrev TestFunction (d : ℕ) : Type := SchwartzMap (SpaceTime d) ℝ
```
**Informal**: Real-valued Schwartz functions on $\mathbb{R}^d$ (smooth, rapidly decreasing together with all derivatives).

---

### [`TestFunction𝕜`](../../OSforGFF/Spacetime/Basic.lean#L92) — Definition

**Lean signature**
```lean
abbrev TestFunction𝕜 (d : ℕ) : Type := SchwartzMap (SpaceTime d) 𝕜
```
**Informal**: $\mathbb{k}$-valued Schwartz functions on $\mathbb{R}^d$, parameterized over an `RCLike` scalar field $\mathbb{k}$.

---

### [`TestFunctionℂ`](../../OSforGFF/Spacetime/Basic.lean#L93) — Definition

**Lean signature**
```lean
abbrev TestFunctionℂ (d : ℕ) := TestFunction𝕜 (𝕜 := ℂ) d
```
**Informal**: Complex-valued Schwartz functions on $\mathbb{R}^d$.

---

### [`TestFunction4`](../../OSforGFF/Spacetime/Basic.lean#L95) — Definition

**Lean signature**
```lean
abbrev TestFunction4 : Type := TestFunction STDimension
```
**Informal**: Real test functions on four-dimensional spacetime, the `d = 4` instance of `TestFunction`.

---

### [`TestFunctionℂ4`](../../OSforGFF/Spacetime/Basic.lean#L96) — Definition

**Lean signature**
```lean
abbrev TestFunctionℂ4 := TestFunctionℂ STDimension
```
**Informal**: Complex test functions on four-dimensional spacetime, the `d = 4` instance of `TestFunctionℂ`.

---

### [`pointwiseMulCLM`](../../OSforGFF/Spacetime/Basic.lean#L106) — Definition

**Lean signature**
```lean
def pointwiseMulCLM : ℂ →L[ℂ] ℂ →L[ℂ] ℂ
```
**Informal**: The bilinear map given by complex multiplication, viewed as a continuous linear map $\mathbb{C} \to (\mathbb{C} \to \mathbb{C})$.

---

### [`schwartzMul`](../../OSforGFF/Spacetime/Basic.lean#L109) — Definition

**Lean signature**
```lean
def schwartzMul (g : TestFunctionℂ d) : TestFunctionℂ d →L[ℂ] TestFunctionℂ d
```
**Informal**: Pointwise multiplication by a fixed Schwartz function $g$, as a continuous linear operator on the Schwartz space.

---

## Glimm-Jaffe Distribution Framework

### [`FieldConfiguration`](../../OSforGFF/Spacetime/Basic.lean#L127) — Definition

**Lean signature**
```lean
abbrev FieldConfiguration (d : ℕ) := WeakDual ℝ (SchwartzMap (SpaceTime d) ℝ)
```
**Informal**: The space of field configurations, modeled as the topological dual $\mathcal{S}'(\mathbb{R}^d)$ of real Schwartz functions, carrying the weak-$*$ topology.

---

### [`FieldConfiguration4`](../../OSforGFF/Spacetime/Basic.lean#L130) — Definition

**Lean signature**
```lean
abbrev FieldConfiguration4 := FieldConfiguration STDimension
```
**Informal**: Field configurations over four-dimensional spacetime, the `d = 4` instance of `FieldConfiguration`.

---

### [`distributionPairing`](../../OSforGFF/Spacetime/Basic.lean#L140) — Definition

**Lean signature**
```lean
def distributionPairing (ω : FieldConfiguration d) (f : TestFunction d) : ℝ
```
**Informal**: The canonical pairing $\langle \omega, f \rangle \in \mathbb{R}$ between a tempered distribution $\omega$ and a Schwartz test function $f$.

---

### [`distributionPairing_add`](../../OSforGFF/Spacetime/Basic.lean#L142) — Lemma

**Statement**: The pairing is additive in the first argument: $\langle \omega_1 + \omega_2, f \rangle = \langle \omega_1, f \rangle + \langle \omega_2, f \rangle$.

**Proof uses**: *(definitional — `rfl`)*

---

### [`distributionPairing_smul`](../../OSforGFF/Spacetime/Basic.lean#L145) — Lemma

**Statement**: Scalar multiplication in the first argument: $\langle s \cdot \omega, f \rangle = s \cdot \langle \omega, f \rangle$ for $s \in \mathbb{R}$.

**Proof uses**: *(definitional — `rfl`)*

---

### [`pairing_smul_real`](../../OSforGFF/Spacetime/Basic.lean#L150) — Lemma

**Statement**: Linearity in the second argument: $\omega(s \cdot f) = s \cdot \omega(f)$ for $s \in \mathbb{R}$.

**Proof uses**: `map_smul`

---

### [`distributionPairingCLM`](../../OSforGFF/Spacetime/Basic.lean#L155) — Definition

**Lean signature**
```lean
def distributionPairingCLM (a : TestFunction d) : FieldConfiguration d →L[ℝ] ℝ
```
**Informal**: For a fixed test function $a$, the evaluation map $\omega \mapsto \langle \omega, a \rangle$ as a continuous linear functional on field configurations (continuous by the weak-$*$ topology via `WeakDual.eval_continuous`).

---

### [`distributionPairingCLM_apply`](../../OSforGFF/Spacetime/Basic.lean#L167) — Lemma

**Statement**: The continuous linear map `distributionPairingCLM a` applied to $\omega$ equals `distributionPairing ω a`.

**Proof uses**: *(definitional — `rfl`)*

---

## Glimm-Jaffe Generating Functional

### [`GJGeneratingFunctional`](../../OSforGFF/Spacetime/Basic.lean#L179) — Definition

**Lean signature**
```lean
def GJGeneratingFunctional (dμ_config : ProbabilityMeasure (FieldConfiguration d))
    (J : TestFunction d) : ℂ
```
**Informal**: The generating functional $Z[J] = \int_{\mathcal{S}'} e^{i\langle\omega, J\rangle}\,d\mu(\omega)$, integrating the phase $e^{i\langle\omega,J\rangle}$ over field configurations with respect to a probability measure.

---

### [`schwartz_comp_clm`](../../OSforGFF/Spacetime/Basic.lean#L185) — Definition

**Lean signature**
```lean
def schwartz_comp_clm (f : TestFunctionℂ d) (L : ℂ →L[ℝ] ℝ) : TestFunction d
```
**Informal**: Given a complex Schwartz function $f$ and a continuous $\mathbb{R}$-linear map $L : \mathbb{C} \to \mathbb{R}$, produces the real Schwartz function $x \mapsto L(f(x))$, using the Schwartz seminorm bound $\lVert L \circ f \rVert \leq \lVert L\rVert \cdot \lVert f\rVert$.

---

### [`schwartz_comp_clm_apply`](../../OSforGFF/Spacetime/Basic.lean#L215) — Lemma

**Statement**: Evaluation of `schwartz_comp_clm f L` at a point: $(\mathrm{schwartz\_comp\_clm}\;f\;L)(x) = L(f(x))$.

**Proof uses**: *(definitional — `rfl`)*

---

### [`complex_testfunction_decompose`](../../OSforGFF/Spacetime/Basic.lean#L220) — Definition

**Lean signature**
```lean
def complex_testfunction_decompose (f : TestFunctionℂ d) : TestFunction d × TestFunction d
```
**Informal**: Decomposes a complex Schwartz function $f$ into its real and imaginary parts $(f_{\mathrm{re}}, f_{\mathrm{im}})$, each a real Schwartz function, via `schwartz_comp_clm` applied to `Complex.reCLM` and `Complex.imCLM`.

---

### [`complex_testfunction_decompose_fst_apply`](../../OSforGFF/Spacetime/Basic.lean#L224) — Lemma

**Statement**: The first component of the decomposition evaluates pointwise to the real part: $(\mathrm{decompose}\,f).1\,(x) = \mathrm{Re}(f(x))$.

**Proof uses**: `schwartz_comp_clm`, `Complex.reCLM`

---

### [`complex_testfunction_decompose_snd_apply`](../../OSforGFF/Spacetime/Basic.lean#L230) — Lemma

**Statement**: The second component evaluates pointwise to the imaginary part: $(\mathrm{decompose}\,f).2\,(x) = \mathrm{Im}(f(x))$.

**Proof uses**: `schwartz_comp_clm`, `Complex.imCLM`

---

### [`complex_testfunction_decompose_fst_apply_coe`](../../OSforGFF/Spacetime/Basic.lean#L236) — Lemma

**Statement**: Coerced to $\mathbb{C}$: $\bigl((\mathrm{decompose}\,f).1\,(x) : \mathbb{C}\bigr) = (\mathrm{Re}(f(x)) : \mathbb{C})$.

**Proof uses**: `complex_testfunction_decompose_fst_apply`

---

### [`complex_testfunction_decompose_snd_apply_coe`](../../OSforGFF/Spacetime/Basic.lean#L242) — Lemma

**Statement**: Coerced to $\mathbb{C}$: $\bigl((\mathrm{decompose}\,f).2\,(x) : \mathbb{C}\bigr) = (\mathrm{Im}(f(x)) : \mathbb{C})$.

**Proof uses**: `complex_testfunction_decompose_snd_apply`

---

### [`complex_testfunction_decompose_recompose`](../../OSforGFF/Spacetime/Basic.lean#L248) — Lemma

**Statement**: Recomposition identity: for any complex Schwartz function $f$ and point $x$,
$$f(x) = f_{\mathrm{re}}(x) + i \cdot f_{\mathrm{im}}(x)$$
where $(f_{\mathrm{re}}, f_{\mathrm{im}}) = \mathrm{decompose}(f)$.

**Proof uses**: `Complex.re_add_im`

---

### [`distributionPairingℂ_real`](../../OSforGFF/Spacetime/Basic.lean#L263) — Definition

**Lean signature**
```lean
def distributionPairingℂ_real (ω : FieldConfiguration d) (f : TestFunctionℂ d) : ℂ
```
**Informal**: The $\mathbb{C}$-valued pairing between a real distribution $\omega$ and a complex test function $f$, defined by $\langle\omega, f\rangle_\mathbb{C} = \langle\omega, f_{\mathrm{re}}\rangle + i\langle\omega, f_{\mathrm{im}}\rangle$.

---

### [`GJGeneratingFunctionalℂ`](../../OSforGFF/Spacetime/Basic.lean#L270) — Definition

**Lean signature**
```lean
def GJGeneratingFunctionalℂ (dμ_config : ProbabilityMeasure (FieldConfiguration d))
    (J : TestFunctionℂ d) : ℂ
```
**Informal**: Complex-source generating functional $Z[J] = \int e^{i\langle\omega,J\rangle_\mathbb{C}}\,d\mu(\omega)$ where $J$ is a complex Schwartz function.

---

### [`GJMean`](../../OSforGFF/Spacetime/Basic.lean#L275) — Definition

**Lean signature**
```lean
def GJMean (dμ_config : ProbabilityMeasure (FieldConfiguration d))
    (φ : TestFunction d) : ℝ
```
**Informal**: The mean field $\langle\phi\rangle = \int \langle\omega,\phi\rangle\,d\mu(\omega)$, i.e., the expectation of the distribution pairing with a test function $\phi$.

---

## Spatial Geometry and Energy Operators

### [`SpatialCoords`](../../OSforGFF/Spacetime/Basic.lean#L282) — Definition

**Lean signature**
```lean
abbrev SpatialCoords (d : ℕ) := EuclideanSpace ℝ (Fin (d - 1))
```
**Informal**: The spatial slice $\mathbb{R}^{d-1}$ (spacetime minus the time direction), as a Euclidean space with $L^2$ norm.

---

### [`SpatialCoords4`](../../OSforGFF/Spacetime/Basic.lean#L285) — Definition

**Lean signature**
```lean
abbrev SpatialCoords4 := SpatialCoords STDimension
```
**Informal**: Spatial coordinates $\mathbb{R}^3$ of the four-dimensional instance.

---

### [`SpatialL2`](../../OSforGFF/Spacetime/Basic.lean#L288) — Definition

**Lean signature**
```lean
abbrev SpatialL2 (d : ℕ) := Lp ℝ 2 (volume : Measure (SpatialCoords d))
```
**Informal**: The $L^2(\mathbb{R}^{d-1})$ space of real-valued square-integrable functions on the spatial slice.

---

### [`SpatialL24`](../../OSforGFF/Spacetime/Basic.lean#L291) — Definition

**Lean signature**
```lean
abbrev SpatialL24 := SpatialL2 STDimension
```
**Informal**: Spatial $L^2(\mathbb{R}^3)$ space of the four-dimensional instance.

---

### [`spatialPart`](../../OSforGFF/Spacetime/Basic.lean#L294) — Definition

**Lean signature**
```lean
def spatialPart (x : SpaceTime d) : SpatialCoords d
```
**Informal**: Extracts the spatial part $(x_1, \ldots, x_{d-1})$ from a spacetime point $x = (x_0, x_1, \ldots, x_{d-1})$.

---

### [`E`](../../OSforGFF/Spacetime/Basic.lean#L299) — Definition

**Lean signature**
```lean
def E (m : ℝ) (k : SpatialCoords d) : ℝ
```
**Informal**: The on-shell energy $E(m,k) = \sqrt{\lVert k\rVert^2 + m^2}$ for a particle of mass $m$ and spatial momentum $k$.

---

*This file has **27** definitions and **10** theorems/lemmas (0 with sorry).*
