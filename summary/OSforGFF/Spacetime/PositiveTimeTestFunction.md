# `PositiveTimeTestFunction.lean` — Informal Summary

> **Source**: [`OSforGFF/Spacetime/PositiveTimeTestFunction.lean`](../../OSforGFF/Spacetime/PositiveTimeTestFunction.lean)
> **Generated**: 2026-07-03 (updated for the dimension-generic library)
> **Note**: Auto-generated by `/lean-summarize`. Re-run to refresh.
> **⚠ Stale (pre–Stage G)**: predates the uniform-`d` refactor and still lists removed identifiers (`STDimension`, the `*4` shorthands, and/or `μ_GFF2`/`μ_GFF3`/`μ_GFF5`). The Lean source is authoritative; regenerate with `/lean-summarize`.

## Overview

Defines the positive-time region and the test functions supported in it, together
with the star operation used for reflection positivity (OS3). The file is
dimension-generic over the section variables `{d : ℕ} [Fact (2 ≤ d)]` (the
$2 \le d$ hypothesis is what makes the time coordinate available). The
positive-time half-space is $\{x \in \mathbb{R}^d \mid x_0 > 0\}$, and
`PositiveTimeTestFunction d` / `PositiveTimeTestFunctionℂ d` are the real and
complex Schwartz functions whose closed support lies in it, realized as
submodules (over $\mathbb{R}$ and $\mathbb{C}$ respectively). The `…4` shorthands
`PositiveTimeTestFunction4` and `PositiveTimeTestFunctionℂ4` name the
`d = STDimension = 4` instance. The star operation `starTestFunction` — time
reflection followed by pointwise complex conjugation — is packaged as a `Star`
instance on `TestFunctionℂ d`; it stays within the Schwartz space because both
steps are smooth isometries.

## Status

**Main result**: Fully proven

None — file is sorry-free.

**Length**: 187 lines, 14 definition(s) + 5 theorem(s)/lemma(s)

---

## Positive-Time Test Functions

### [`HasPositiveTime`](../../OSforGFF/Spacetime/PositiveTimeTestFunction.lean#L44) — Definition

**Lean signature**
```lean
def HasPositiveTime (x : (SpaceTime d)) : Prop := getTimeComponent x > 0
```
**Informal**: Predicate that a spacetime point has strictly positive time, i.e. its zeroth (time) coordinate satisfies $x_0 > 0$.

---

### [`positiveTimeSet`](../../OSforGFF/Spacetime/PositiveTimeTestFunction.lean#L47) — Definition

**Lean signature**
```lean
def positiveTimeSet : Set (SpaceTime d) := {x | HasPositiveTime x}
```
**Informal**: The positive-time half-space $\{x \in \mathbb{R}^d \mid x_0 > 0\}$.

---

### [`is_open_positiveTimeSet`](../../OSforGFF/Spacetime/PositiveTimeTestFunction.lean#L50) — Lemma

**Statement**: The positive-time set is open — it is the preimage of $(0, \infty)$ under the continuous time-coordinate projection $x \mapsto x_0$.

**Proof uses**: `isOpen_lt`, `PiLp.continuous_apply`

---

### [`PositiveTimeTestFunctions.submodule`](../../OSforGFF/Spacetime/PositiveTimeTestFunction.lean#L55) — Definition

**Lean signature**
```lean
def PositiveTimeTestFunctions.submodule : Submodule ℝ (TestFunction d)
```
**Informal**: The $\mathbb{R}$-submodule of real test functions whose closed support $\mathrm{tsupport}\,f$ is contained in the positive-time set; membership is closed under addition and scalar multiplication because support only shrinks under these operations.

---

### [`PositiveTimeTestFunction`](../../OSforGFF/Spacetime/PositiveTimeTestFunction.lean#L70) — Definition

**Lean signature**
```lean
abbrev PositiveTimeTestFunction (d : ℕ) [Fact (2 ≤ d)] : Type :=
  PositiveTimeTestFunctions.submodule (d := d)
```
**Informal**: The type of real test functions supported in the positive-time region, i.e. the elements of `PositiveTimeTestFunctions.submodule`.

---

### [`PositiveTimeTestFunction4`](../../OSforGFF/Spacetime/PositiveTimeTestFunction.lean#L74) — Definition

**Lean signature**
```lean
abbrev PositiveTimeTestFunction4 : Type := PositiveTimeTestFunction STDimension
```
**Informal**: Positive-time real test functions on four-dimensional spacetime, the `d = STDimension = 4` instance of `PositiveTimeTestFunction`.

---

### [`AddCommMonoid (PositiveTimeTestFunction d)`](../../OSforGFF/Spacetime/PositiveTimeTestFunction.lean#L76) — Instance

**Lean signature**
```lean
instance : AddCommMonoid (PositiveTimeTestFunction d)
```
**Informal**: Additive commutative monoid structure on the positive-time real test functions, inferred from the submodule structure.

---

### [`AddCommGroup (PositiveTimeTestFunction d)`](../../OSforGFF/Spacetime/PositiveTimeTestFunction.lean#L77) — Instance

**Lean signature**
```lean
instance : AddCommGroup (PositiveTimeTestFunction d)
```
**Informal**: Additive commutative group structure on the positive-time real test functions.

---

### [`PositiveTimeTestFunction.sum_smul_mem`](../../OSforGFF/Spacetime/PositiveTimeTestFunction.lean#L80) — Lemma

**Statement**: Finite linear combinations stay in the region: for real test functions $f_i$ supported in positive time and scalars $c_i$, there is a positive-time test function $g$ with $g = \sum_i c_i \cdot (f_i)$.

**Proof uses**: submodule closure under finite sums (`Submodule.coe_sum`, `Submodule.coe_smul_of_tower`)

---

### [`PositiveTimeTestFunctionsℂ.submodule`](../../OSforGFF/Spacetime/PositiveTimeTestFunction.lean#L90) — Definition

**Lean signature**
```lean
def PositiveTimeTestFunctionsℂ.submodule : Submodule ℂ (TestFunctionℂ d)
```
**Informal**: The $\mathbb{C}$-submodule of complex test functions whose closed support is contained in the positive-time set; $\mathbb{C}$-scalar multiplication preserves support, so this is a genuine $\mathbb{C}$-submodule.

---

### [`PositiveTimeTestFunctionℂ`](../../OSforGFF/Spacetime/PositiveTimeTestFunction.lean#L105) — Definition

**Lean signature**
```lean
abbrev PositiveTimeTestFunctionℂ (d : ℕ) [Fact (2 ≤ d)] : Type :=
  PositiveTimeTestFunctionsℂ.submodule (d := d)
```
**Informal**: The type of complex test functions supported in the positive-time region.

---

### [`PositiveTimeTestFunctionℂ4`](../../OSforGFF/Spacetime/PositiveTimeTestFunction.lean#L109) — Definition

**Lean signature**
```lean
abbrev PositiveTimeTestFunctionℂ4 : Type := PositiveTimeTestFunctionℂ STDimension
```
**Informal**: Positive-time complex test functions on four-dimensional spacetime, the `d = STDimension = 4` instance of `PositiveTimeTestFunctionℂ`.

---

### [`AddCommMonoid (PositiveTimeTestFunctionℂ d)`](../../OSforGFF/Spacetime/PositiveTimeTestFunction.lean#L111) — Instance

**Lean signature**
```lean
instance : AddCommMonoid (PositiveTimeTestFunctionℂ d)
```
**Informal**: Additive commutative monoid structure on the positive-time complex test functions.

---

### [`AddCommGroup (PositiveTimeTestFunctionℂ d)`](../../OSforGFF/Spacetime/PositiveTimeTestFunction.lean#L112) — Instance

**Lean signature**
```lean
instance : AddCommGroup (PositiveTimeTestFunctionℂ d)
```
**Informal**: Additive commutative group structure on the positive-time complex test functions.

---

### [`PositiveTimeTestFunctionℂ.zero_on_nonpositive`](../../OSforGFF/Spacetime/PositiveTimeTestFunction.lean#L114) — Lemma

**Statement**: A complex positive-time test function vanishes off the region: if $x_0 \le 0$ then $f(x) = 0$ (the point lies outside the closed support).

**Proof uses**: `image_eq_zero_of_notMem_tsupport`, the support condition `f.property`

---

## Star Operation on Test Functions

### [`starRingEnd_iteratedFDeriv_norm_eq`](../../OSforGFF/Spacetime/PositiveTimeTestFunction.lean#L129) — Lemma

**Statement**: Complex conjugation commutes through iterated derivatives and preserves their norms: $\lVert D^n(\overline{g})(x)\rVert = \lVert D^n g(x)\rVert$ (stated with `[Fact (2 ≤ d)]` omitted).

**Proof uses**: `Complex.conjLIE`, `LinearIsometryEquiv.norm_iteratedFDeriv_comp_left`

---

### [`starTestFunction`](../../OSforGFF/Spacetime/PositiveTimeTestFunction.lean#L142) — Definition

**Lean signature**
```lean
noncomputable def starTestFunction (f : (TestFunctionℂ d)) : (TestFunctionℂ d)
```
**Informal**: The star operation on complex test functions: time reflection (`compTimeReflection`) followed by pointwise complex conjugation, $(\star f)(x) = \overline{f(\theta x)}$; the result is again Schwartz because both steps are smooth isometries, using `starRingEnd_iteratedFDeriv_norm_eq` for the decay bounds.

---

### [`Star (TestFunctionℂ d)`](../../OSforGFF/Spacetime/PositiveTimeTestFunction.lean#L171) — Instance

**Lean signature**
```lean
noncomputable instance : Star (TestFunctionℂ d)
```
**Informal**: The `Star` typeclass instance on complex test functions, defined by `star f = starTestFunction f`.

---

### [`PositiveTimeTestFunction.zero_on_nonpositive`](../../OSforGFF/Spacetime/PositiveTimeTestFunction.lean#L174) — Lemma

**Statement**: A real positive-time test function vanishes off the region: if $x_0 \le 0$ then $f(x) = 0$.

**Proof uses**: `image_eq_zero_of_notMem_tsupport`, the support condition `f.property`

---

*This file has **14** definition(s) and **5** theorems/lemmas (0 with sorry).*
